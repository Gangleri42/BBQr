# BBQr file type M: Shamir shares

The specification moved to the repository root: **[SHAMIR.md](../../SHAMIR.md)**.

This Go package (`shamir`) is one of its reference implementations; the
other is `python/bbqr/shamir.py`. Cross-implementation test vectors are
`test_data/shamir_vectors.json` at the root, mirrored here under
`testdata/vectors.json` for the module's self-contained tests.
